<div id="comments">
  <p class="commenter"><%= comment.user_id %></p><% if is_admin? %><%= link_to 'Delete comment', [comment.event, comment], :method => :delete %><% end %>
  <p class="comment-updated_at"><%= comment.updated_at %>
  <p class="comment-body"><%= comment.body %></p>
</div>
