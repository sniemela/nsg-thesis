<div id="comments">
  <p class="comment-commenter"><%= comment.user_id %></p>
  <p class="comment-created_at"><%= comment.created_at %></p>
  <p class="comment-body"><%= comment.body %></p>
  <% if is_admin? %>
    <%= link_to "Remove", [@event, comment], :method => :destroy %>
  <% end %>
</div>
